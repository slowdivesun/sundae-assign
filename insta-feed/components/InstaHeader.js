"use client";

import { Typography } from "@mui/material";

const LoginButton = () => {
  return (
    <Typography variant='h3' gutterBottom>
      Welcome
    </Typography>
  );
};

export default LoginButton;
